// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("class_ext", "for class_ext support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("class_ext", "enable class_ext support", "no");

if (PHP_CLASS_EXT != "no") {
	EXTENSION("class_ext", "class_ext.c");
}

