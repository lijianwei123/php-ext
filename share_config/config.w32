// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("share_config", "for share_config support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("share_config", "enable share_config support", "no");

if (PHP_SHARE_CONFIG != "no") {
	EXTENSION("share_config", "share_config.c");
}

