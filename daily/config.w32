// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("daily", "for daily support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("daily", "enable daily support", "no");

if (PHP_DAILY != "no") {
	EXTENSION("daily", "daily.c");
}

